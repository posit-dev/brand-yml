meta:
  name: Tests that paths are resolved correctly

logo:
  images:
    missing: does-not-exist.png
  small: missing

typography:
  fonts:
    - family: Invisible
      source: file
      files:
        - path: Invisible.ttf
  base: Invisible
