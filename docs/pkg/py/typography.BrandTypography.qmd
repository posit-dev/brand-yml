# typography.BrandTypography { #brand_yaml.typography.BrandTypography }

`typography.BrandTypography()`



## Attributes

| Name | Description |
| --- | --- |
| [model_config](#brand_yaml.typography.BrandTypography.model_config) | dict() -> new empty dictionary |

## Methods

| Name | Description |
| --- | --- |
| [forward_monospace_values](#brand_yaml.typography.BrandTypography.forward_monospace_values) | Forward values from `monospace` to inline and block variants. |

### forward_monospace_values { #brand_yaml.typography.BrandTypography.forward_monospace_values }

`typography.BrandTypography.forward_monospace_values()`

Forward values from `monospace` to inline and block variants.

`monospace-inline` and `monospace-block` both inherit `family`, `style`,
`weight` and `size` from `monospace`.