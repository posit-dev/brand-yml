# Brand { #brand_yaml.Brand }

`Brand()`



## Methods

| Name | Description |
| --- | --- |
| [from_yaml](#brand_yaml.Brand.from_yaml) | Read a brand YAML file |
| [from_yaml_str](#brand_yaml.Brand.from_yaml_str) | Create a Brand instance from a YAML string |

### from_yaml { #brand_yaml.Brand.from_yaml }

`Brand.from_yaml(path)`

Read a brand YAML file

Reads a brand YAML file or finds and reads a `_brand.yml` file and returns
a validated :class:`Brand` object.

#### Parameters

| Name   | Type                                   | Description                                                                                                                                                                                                                           | Default    |
|--------|----------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------|
| `path` | [str](`str`) \| [Path](`pathlib.Path`) | The path to the brand YAML file or a directory where `_brand.yml` is expected to be found. Typically, you can pass `__file__` from the calling script to find `_brand.yml` in the current directory or any of its parent directories. | _required_ |

#### Returns

| Type   | Description                                                                                   |
|--------|-----------------------------------------------------------------------------------------------|
|        | A validated :class:`Brand` object with all fields populated according to the brand YAML file. |

#### Raises

| Type   | Description                                                                                                                                                                                                                      |
|--------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| :      | Raises a `FileNotFoundError` if no brand configuration file is found within the given path. Raises `ValueError` or other validation errors from [pydantic](https://docs.pydantic.dev/latest/) if the brand YAML file is invalid. |

#### Examples

```python
from brand_yaml import Brand

brand = Brand.from_yaml(__file__)
brand = Brand.from_yaml("path/to/_brand.yml")
```

### from_yaml_str { #brand_yaml.Brand.from_yaml_str }

`Brand.from_yaml_str(text, path=None)`

Create a Brand instance from a YAML string

#### Parameters

| Name   | Type                                           | Description                                                          | Default    |
|--------|------------------------------------------------|----------------------------------------------------------------------|------------|
| `text` | [str](`str`)                                   | The text of the brand YAML file.                                     | _required_ |
| `path` | [str](`str`) \| [Path](`pathlib.Path`) \| None | The optional path on disk for supporting files like logos and fonts. | `None`     |

#### Returns

| Type   | Description                                                                                   |
|--------|-----------------------------------------------------------------------------------------------|
|        | A validated :class:`Brand` object with all fields populated according to the brand YAML text. |

#### Raises

| Type   | Description                                                                                                                          |
|--------|--------------------------------------------------------------------------------------------------------------------------------------|
| :      | Raises `ValueError` or other validation errors from [pydantic](https://docs.pydantic.dev/latest/) if the brand YAML file is invalid. |

#### Examples

```python
from brand_yaml import Brand

brand = Brand.from_yaml_str("""
meta:
  name: Brand YAML
color:
  primary: "#ff0202"
typography:
  base: Open Sans
""")
```