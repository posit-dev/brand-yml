# BrandTypography { #brand_yaml.BrandTypography }

`BrandTypography()`




## Methods

| Name | Description |
| --- | --- |
| [forward_monospace_values](#brand_yaml.BrandTypography.forward_monospace_values) | Forward values from `monospace` to inline and block variants. |

### forward_monospace_values { #brand_yaml.BrandTypography.forward_monospace_values }

`BrandTypography.forward_monospace_values()`

Forward values from `monospace` to inline and block variants.

`monospace-inline` and `monospace-block` both inherit `family`, `style`,
`weight` and `size` from `monospace`.